export const HEADING_RE = /^#{1,6}\s/;
export const LIST_MARKER_RE = /^\s*([-*+]|\d+\.)\s/;
export const INLINE_TOKEN_RE = /(\*\*[^*]+\*\*|`[^`]+`)/g;
export const BOLD_RE = /^\*\*[^*]+\*\*$/;
export const INLINE_CODE_RE = /^`[^`]+`$/;

export const DEFAULT_EDITOR_HEIGHT = 360;
